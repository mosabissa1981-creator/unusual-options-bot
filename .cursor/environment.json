{
  "name": "unusual-options-bot",
  "install": "python3 -m venv .venv && .venv/bin/pip install -U pip && .venv/bin/pip install -r requirements.txt",
  "terminals": [
    {
      "name": "web-scanner",
      "command": ".venv/bin/streamlit run streamlit_app.py --server.port=8080 --server.address=0.0.0.0 --server.headless=true --browser.gatherUsageStats=false"
    }
  ]
}
